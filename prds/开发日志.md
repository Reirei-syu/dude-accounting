# 开发日志

2026-03-10

- 参照报表查询页的冻结交互结构重构凭证管理页顶部交互：原交互块改为 `glass-panel-light` 标题/说明 + 按钮布局，新增滚动时渐显的顶部副本按钮栏，沿用同样的渐隐/渐显与模糊遮罩方案。
- 凭证管理页补充独立信息交互块，集中展示“当前筛选/已勾选”统计、结账只读提示和最近刷新摘要。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 调整凭证管理页根容器滚动行为，为 `VoucherList` 页面根容器增加 `overflow-y-auto overflow-x-hidden`，使主工作区域在内容超高时显示上下滑块。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 重构企业账套报表输出模块，按系统级 `qiye-cas-report-templates` Skill 的官方制式输出资产负债表、利润表、现金流量表，并新增所有者权益变动表 `equity_statement` 报表类型与生成能力。
- 企业资产负债表改为官方左右并列表样，利润表/现金流量表改为官方表头与行次，所有者权益变动表改为“本年金额 + 上年金额”双年度纵向分块矩阵，并同步接入报表查询、预览、Excel/PDF 导出与企业报表菜单入口。
- 同步扩展 `report_snapshots` 表的 `report_type` 约束与旧库迁移逻辑，更新 main/preload/renderer 三层 `ReportType`、组件映射、菜单配置和查询筛选类型。
- 补充并更新企业报表与筛选测试，覆盖官方资产负债表并列表样、利润表动态范围、现金流量表导出、所有者权益变动表双年度分块，以及新增 `equity_statement` 类型筛选。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 继续细化系统级技能 `C:\Users\11441\.codex\skills\qiye-cas-report-templates\SKILL.md` 的会企04表，改为官方常见印刷版“本年金额 + 上年金额”双年度纵向分块矩阵，并补入 `一般风险准备` 列及对应利润分配行。
- 同步重构 `C:\Users\11441\.codex\skills\qiye-cas-report-templates\references\report-templates.json` 的所有者权益变动表参考结构，改为 `shared_columns + year_blocks + rows`，明确共享列结构与双年度区块。
- 同步更新 `C:\Users\11441\.codex\skills\qiye-cas-report-templates\references\output-rules.md` 与 `official-basis.md`，将会企04表默认输出和校验口径固定为双年度分块矩阵。
- 完成验证：`npm run typecheck`、`npm test`；`quick_validate.py C:\Users\11441\.codex\skills\qiye-cas-report-templates` 因本机 Python 缺少 `yaml` 依赖未能执行通过。

2026-03-10

- 继续优化系统级技能 `C:\Users\11441\.codex\skills\qiye-cas-report-templates\SKILL.md` 的会企01表制式，改为官方左右并列表样：左侧“资产”，右侧“负债和所有者权益（或股东权益）”，不再使用上下串联的两段写法。
- 同步更新 `C:\Users\11441\.codex\skills\qiye-cas-report-templates\references\report-templates.json` 与 `output-rules.md`，补充资产负债表左右并列展示元数据和 markdown / Excel 默认输出规则，保证技能描述层与参考模板层一致。
- 完成验证：`npm run typecheck`、`npm test`；`quick_validate.py C:\Users\11441\.codex\skills\qiye-cas-report-templates` 因本机 Python 缺少 `yaml` 依赖未能执行通过。

2026-03-10

- 更新系统级技能 `C:\Users\11441\.codex\skills\qiye-cas-report-templates\SKILL.md`，将一般企业《企业会计准则》会企 01/02/03/04 表固化为 canonical report forms，并固定双列/分栏列示结构、比较期列头与输出约束。
- 依据财政部一般企业财务报表格式口径补充技能级冻结规则：会企01表采用“期末余额 / 上年年末余额”，会企02/03表采用“本期金额 / 上期金额”，会企04表采用“本年金额 / 上年金额”矩阵结构，并明确草稿错项不得覆盖官方制式。
- 完成验证：`npm run typecheck`、`npm test`；`quick_validate.py C:\Users\11441\.codex\skills\qiye-cas-report-templates` 因本机 Python 缺少 `yaml` 依赖未能执行通过。

2026-03-09

- 更新系统级技能 `C:\Users\11441\.codex\skills\ngo-report-templates\SKILL.md`，将当前已落地的民非报表格式固定为技能约束，补充导出标题区、会计期间中文格式、业务活动表列头断行、科目/现金流显式映射规则，以及会民非 01/02/03 表的 canonical report forms。
- 完成验证：`npm run typecheck`、`npm test`；`quick_validate.py C:\Users\11441\.codex\skills\ngo-report-templates` 因本机 Python 缺少 `yaml` 依赖未能执行通过。

2026-03-09

- 将报表查询页渐显副本按钮层提升为最高优先级冻结遮罩：固定于顶部、提高 z-index，并为其上层容器增加高透明背景、底边框、投影与强 `backdrop blur`，避免下方内容穿透影响按钮可读性。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 调整报表查询页渐显副本按钮栏为左侧贴齐布局，并将副本上层容器的背景、边框、阴影改为透明，仅保留按钮本身用于冻结态批量操作。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 重新实现报表查询页冻结副本按钮布局：副本层改为与原交互块同宽、同内边距、同右侧按钮布局，不再依赖手工偏移补偿，以保证按钮相对容器的位置一致。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 进一步修正报表查询页渐显副本按钮栏的位置，按原功能按钮组中首个“刷新清单”按钮左边框作为基线，对副本按钮栏做同基线对齐。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 调整报表查询页渐显副本按钮栏的停靠方式与横向内边距，改为以原按钮组左边界为基准对齐，减少冻结态按钮与原按钮组的水平偏差。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 优化报表查询页冻结交互：顶部完整“报表查询”交互块随下拉滚动渐隐，独立按钮栏在容器上沿附近渐显并保持可操作，冻结结果仅保留批量操作按钮。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 报表查询页新增批量删除：多选勾选状态下可直接批量删除已选报表，单选时仍保留原删除行为。
- 将报表查询页“会计期间筛选”改为 6 个下拉框的起止日期选择器，默认范围为“本年 1 月 1 日 - 本月最后一天”，并按报表实际期间范围过滤清单。
- 报表查询页顶部“报表查询”工具块改为滚动时粘附在顶部显示，形成类似冻结窗口的效果。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 业务活动表列头改为在全角括号 `（` 前插入换行点，使“本月数/本年累计数”与“（限定性/非限定性/合计）”分两行显示，避免在括号内部错误折行。
- 同步调整页面预览、HTML/PDF 导出和 Excel 导出对该换行的渲染方式；Excel 表头启用自动换行并适当增高表头行。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 报表查询页新增多选勾选框，支持按勾选结果批量导出 Excel/PDF；单击行仍用于详情选中，双击仍用于打开报表详情。
- 新增 `reporting:exportBatch` IPC，批量导出时改为一次选择目录后批量落文件，减少重复另存为操作。
- 报表导出新增最近目录记忆：单个导出和批量导出成功后都会记录最近一次导出目录，后续另存为默认落在上次路径。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 调整报表导出格式：标题区仅保留报表名称、会计期间、编制单位、单位：元，移除取数范围、统计口径、导出时间及表码显示。
- 新增导出专用会计期间中文格式化规则：静态表导出为“YYYY年M月D日”，动态表按单月、同年区间、跨年区间分别输出中文月份范围。
- 调整 HTML/PDF 与 Excel 导出表头样式，首行列表头统一改为水平居中、垂直居中；并补充导出回归测试覆盖 HTML 标题区、动态期间格式与 Excel 标题/表头对齐。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 调整现金流量分配弹窗中“借/贷”方向标记与金额数字之间的显示间距，改为固定 `2ch` 留白，便于金额识别。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 调整记账凭证页“现金流量分配”弹窗的列宽分配，压缩“会计科目”“金额”列宽，为“现金流量项目”列释放更多显示空间。
- 为现金流量分配弹窗新增横向滚动保底方案，内容超宽时可左右滑动查看完整项目名称。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 将记账凭证页底部“制单 / 审核 / 记账”签名区的标签间距从 `96px` 进一步调整为 `200px`，用于观察超大间距下的布局效果。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 将记账凭证页底部“制单 / 审核 / 记账”签名区的标签间距从 `32px` 调整为 `96px`，用于观察更大留白下的展示效果。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 记账凭证页复用“现金流量分配”入口支持只读查看：查看模式和闭账只读模式下可打开现金流量分配弹窗查看结果，但禁用勾选、项目选择和确认分配。
- 扩展 `voucher:list` 返回的凭证元数据，新增制单、审核、记账账号显示名；凭证页底部改为按真实 `creator/auditor/bookkeeper` 显示，不再错误复用当前登录账号。
- 调整凭证页底部“制单：审核：记账：”布局，整体向中部收拢并拉开间距，避免贴近边缘和相互拥挤。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 凭证管理在闭账状态下恢复双击只读查看能力，仍保留“修改”入口受闭账控制，避免已结账期间误编辑。
- 修正民非报表取数映射：资产负债表模板行改为兼容父级科目下新增明细科目余额汇总，解决 `1002` 下新增 `100201` 后未纳入报表的问题。
- 重写民非现金流量表取数分类逻辑，改为“优先按对方科目前缀、兜底按现金流量项目 code/name 别名”识别，不再依赖现金流量项目名称完全相等。
- 为民非报表新增回归测试，覆盖新增明细科目汇总、业务活动表前缀取数、以及通用 `CF03/CF07` 现金流项目映射到会民非03表行。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 优化凭证录入默认日期逻辑：新建记账凭证时，默认日期改为当前期间上一张凭证日期；若当前期间暂无凭证，则默认当月 1 日。
- 新增前端凭证排序工具与测试，统一凭证管理列表、凭证录入导航顺序为“记账凭证在前、结账凭证在后”，并在各自字号内按凭证号升序排列。
- 凭证录入模块的“上一张/下一张”导航改为按当前期间排序序列取数，避免日期优先排序导致的切换错位。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-05

- 将账套设置分支最后两次提交汇入 master
  2026-03-05
- 凭证录入页在日期与字号之间新增“现金流量分配”独立按钮。
- 新增现金流量分配弹窗，支持勾选需分配的现金流分录并选择现金流量项目。
- 移除行内“流”按钮弹层，改为统一弹窗分配流程，并保留分录“已分配/待分配”状态提示。
- 完成 VoucherEntry 页面静态校验：npx eslint src/renderer/src/pages/VoucherEntry.tsx 与 npm run typecheck:web。
  2026-03-05
- 凭证管理新增“修改”操作：支持勾选单张后点击“修改”及双击凭证行进入凭证录入编辑态；仅允许未审核凭证编辑。
- 凭证录入支持编辑态加载指定凭证，并新增“上一张/下一张”切换。
- 切换凭证时新增未保存拦截：弹窗确认后先保存，保存成功才允许切换。
- 新增 `voucher:update` IPC（main/preload/类型声明）并复用原校验规则更新凭证分录。
- 完成验证：`npm run typecheck`、`npm test`；并排查 `npm run dev` 启动条件（需确保未设置 `ELECTRON_RUN_AS_NODE`）。
  2026-03-05
- 修复凭证录入“上一张/下一张”切换逻辑：导航序列改为当前账套全部未审核凭证，并对编辑凭证ID做数值化，避免导航索引失效。
- 切换凭证时增加导航序列失效提示；未保存时继续执行“先确认、先保存、保存成功后切换”。
- 凭证管理新增“刷新”按钮；刷新后对比上次快照并展示新增、减少、状态变更及状态数量增减概况。
- 完成验证：`npm run typecheck`、`npm test`。
  2026-03-05
- 期初数录入模块初步完成。
  2026-03-05
- 现金流量匹配模块开发完成。
  2026-03-05
- 彻底修复开发日志乱码：`prds/开发日志.md` 恢复为正常 UTF-8 文本，并整理为可读中文记录。
- 新增 Git 合并规则：`prds/开发日志.md` 使用 `merge=union`，rebase/merge 时自动合并双方新增日志，避免再次因日志冲突中断。
  2026-03-05
- 新增项目级规范文档 `prds/PROJECT_SPEC.md`，沉淀当前真实架构、模块、数据模型、IPC 接口、约束与未来模块清单，作为跨对话统一背景基线。
- 更新 `AGENTS.md`：新增“每次新对话先读取 `prds/PROJECT_SPEC.md`，重大变更先更新 spec 再开发”的执行规则。
- 完成验证：`npm test`（4 个测试文件 / 17 个用例通过）与 `npm run typecheck`（node/web 均通过）。
  2026-03-05
- 将提交 `e68431b013b1ab96dc5b656a366869a9dc512f08` 与 `62b3440690a4273cb4d2110b5535108f238d703b` 并入 `master`（处理并解决冲突后完成）。
- 按要求执行 `master` 到 `功能/账套设置` 的 rebase；重复补丁自动去重，保留有效变更并完成历史整理。
- 统一修正 `.gitattributes` 为有效规则：`prds/开发日志.md text eol=lf merge=union`。
- 完成验证：`npm test`（4 个测试文件 / 17 个用例通过）。
  2026-03-05
- 修复凭证录入页“上一张/下一张”按钮禁用问题：在新建态也会预加载当前账套未审核凭证序列，允许直接从录入页向前切换到已有凭证。
- 调整导航判定：新建态将“当前位置”视为序列末尾，`上一张`按序可用，`下一张`仅在存在后续凭证时可用。
- 修复新建凭证时重置逻辑：`resetVoucher` 不再清空可编辑凭证序列，避免按钮被错误永久禁用。
- 保存/更新凭证后刷新未审核凭证序列，确保导航状态与最新数据一致。
- 完成验证：`npm run typecheck`、`npm run test`（4 个测试文件 / 17 个用例通过）。
  2026-03-05
- 凭证录入新增“会计期间日期边界”限制：日期输入仅允许当前会计期间内（当月首日~末日），前端输入与保存前校验同时生效。
- 凭证录入交互新增状态显示：增加“已保存 / 编辑中”状态标识并显示在页面顶部。
- 已保存凭证默认只读：新增“编辑”按钮，需点击后才可修改日期、分录和现金流量分配，避免误触。
- 已保存凭证保存后自动回到“已保存”只读态；新建凭证保持“编辑中”。
- 凭证导航列表改为按当前会计期间加载未审核凭证，确保上一张/下一张与当前期间一致。
- 后端新增期间保护：`voucher:getNextNumber`、`voucher:save`、`voucher:update` 均校验凭证日期期间必须等于账套当前会计期间，防止绕过前端提交越界日期。
- 完成验证：`npm run typecheck`、`npm run test`（4 个测试文件 / 17 个用例通过）。
  2026-03-05
- 凭证管理删除能力增强：`voucher:batchAction` 在删除后按“账套+期间+字”自动重排凭证号，确保从 1 开始连续且无断号。
- 凭证录入现金流提示优化：当同一凭证仅为两条现金流科目对冲分录（内部现金划转）时，不再显示“待分配现金流”提示。
- 凭证查询交互优化：支持双击查询结果行直接进入凭证录入页查看指定凭证。
- 凭证查询筛选项收敛：移除“摘要关键字”输入与前端查询参数，避免误录入。
- 修复并清理 `VoucherEntry.tsx` 中历史字符串破损导致的语法问题，恢复可编译状态。
- 完成验证：`npm run typecheck`、`npm test`（4 个测试文件 / 17 个用例通过）。
  2026-03-05
- 修复凭证录入页面乱码：清理 `VoucherEntry.tsx` 中全部异常文案，恢复按钮、表头、提示、状态文本为正常中文显示。
- 恢复凭证查询“摘要关键字”模糊查询：`VoucherQuery.tsx` 重新加入摘要输入框，并将 `keyword` 参数传入 `voucher.list`。
- 保留凭证查询双击进入凭证录入查看功能不变。
- 完成验证：`npm run typecheck`、`npm test`（4 个测试文件 / 17 个用例通过）。
  2026-03-06
- 新增期末损益结转服务与 IPC：支持企业账套结转至本年利润、民非账套按规则结转至对应净资产科目。
- 新增损益结转规则预览页与执行页，支持当前期间结转预览、草稿凭证重建、以及已审核/已记账凭证禁止重跑。
- 期间结账新增损益结转完成校验：当前期间存在应结转金额时，必须先完成并记账损益结转凭证后才能结账。
- 更新 `prds/PROJECT_SPEC.md`，补充期末损益结转模块、IPC 接口与结账约束。
- 完成验证：`npm test`、`npm run typecheck`、定向 `eslint`（本次修改文件通过）。
  2026-03-06
- 修复期末损益结转页纵向溢出：将工作区页签内容容器改为可纵向滚动，超出内容可通过滚轮或拖曳滚动条查看。
- 调整 PLSettle 页面高度策略：由固定满高改为最小满高，移除预览区强制占满剩余空间，避免下方卡片被挤压裁切。
- 将损益结转凭证表与预览分录表改为以页面滚动为主，仅保留横向自适应滚动。

2026-03-06

- 优化凭证录入科目联想：数字输入改为按科目代码/名称前缀检索，并且联想结果仅保留末级科目。
- 凭证保存与修改新增后端末级科目校验，阻止非末级科目写入分录。
- 凭证录入支持回车自动选中当前联想列表首个末级科目，并在科目录入框右侧新增“+”按钮。
- 新增手动选科目弹窗：以默认收起的树形结构展示科目，支持展开后选择末级科目，并提供滚动条承载长列表。
- 更新 `prds/PROJECT_SPEC.md`，补充凭证录入科目选择的新交互与约束。
- 完成验证：`npm run typecheck`、`npm test`、定向 `npx eslint src/main/ipc/subject.ts src/main/ipc/voucher.ts src/renderer/src/pages/VoucherEntry.tsx`。

2026-03-06

- 优化凭证录入科目索引层级展示：联想列表按科目级次增加前置缩进，一级科目不缩进，二级起按每级 2 个字符宽度递增。
- 优化手动选科目弹窗层级展示：弹窗内科目代码列与名称列同步按科目级次缩进，保持树形层级辨识度。
- 将凭证录入行右侧“+”按钮改为独立悬浮玻璃按钮，不再作为会计科目录入框的一部分。
- 完成验证：`npm run typecheck`、`npm test`、定向 `npx eslint src/renderer/src/pages/VoucherEntry.tsx`。

2026-03-06

- 修复凭证录入科目缩进未生效问题：联想列表改为显式 2 字符宽度缩进，不再依赖不稳定的 `padding-left + level` 组合。
- 修复手动选科目弹窗层级识别：新增按科目编码最长前缀推断父级的逻辑，旧账套中 `parent_code/level` 缺失时仍能正确显示缩进与展开关系。
- 同步收紧末级科目识别：前端联想与后端末级校验均增加编码前缀兜底，避免 `1002` 存在 `100201` 时仍被误判为末级。
- 完成验证：`npx eslint src/renderer/src/pages/VoucherEntry.tsx src/main/ipc/subject.ts src/main/ipc/voucher.ts`、`npm run typecheck`、`npm test`（5 个测试文件 / 22 个用例通过）。

2026-03-06

- 修复凭证录入联想列表父级科目缺失问题：科目录入联想改为显示全部匹配科目，不再只保留末级科目。
- 保持末级录入约束不变：联想列表中的父级科目标记为“上级”且不可点击，回车仍自动选中当前联想结果中的第一个末级科目。
- 完成验证：`npx prettier --write src/renderer/src/pages/VoucherEntry.tsx`、`npx eslint src/renderer/src/pages/VoucherEntry.tsx`、`npm run typecheck`、`npm test`（5 个测试文件 / 22 个用例通过）。

2026-03-06

- 凭证录入“上一张/下一张”导航改为按当前账套全部凭证构建，不再只限未审核凭证；从录入页直接进入时也可顺序查看已有凭证，并在新建凭证未保存时切换前给出丢失确认。
- 凭证管理取消双击查看前的未审核限制，未审核、已审核、已记账凭证均可双击进入凭证录入页查看。
- 凭证录入查看态统一保留“修改”按钮：未审核凭证可点按进入修改，已审核或已记账凭证显示为禁用灰态而非隐藏；凭证查询入口进入后同样沿用该规则。

2026-03-06

- 调整凭证查询日期范围默认值：默认改为“本月 1 日到今日”，不再默认到当月最后一天。
- 为凭证查询新增右下角悬浮按钮“本年”，点击后自动将日期范围切换为当年 1 月 1 日到 12 月 31 日。
- 顺手清理 `VoucherQuery.tsx` 页面中的历史乱码文案，恢复查询页标题、筛选项、表头与空状态为正常中文显示。

2026-03-06

- 调整凭证查询“本年”按钮位置：从页面右下角悬浮态改为放入查询表单内部，位于日期范围输入框与摘要关键字输入框之间。

2026-03-06

- 凭证管理新增“全选/取消全选”能力，可对当前列表凭证一键批量选择后执行审核、记账、反审核、反记账等操作。
- 批量审核、记账、反记账、反审核改为仅处理符合当前业务状态的凭证；已不符合条件的凭证自动跳过，不再导致整批操作失败。
- 凭证管理批量操作反馈新增“实际处理数/跳过数”提示，便于区分本次真正生效的凭证数量。

2026-03-06

- 调整凭证管理选择交互：批量审核、记账、反审核、反记账、删除完成后不再主动清空已勾选凭证，仅在列表刷新后自动移除已不存在的行。
- 将凭证管理“全选”入口改为表头“选择”左侧的同尺寸复选框，与各行选择框纵向对齐，不再显示工具栏中的独立全选按钮。
- 顺手收紧批量状态流转：多选时若夹杂不符合条件的凭证，后端按可处理项执行并跳过其余项，避免在保留勾选状态后再次触发整批报错。

2026-03-06

- 为凭证管理新增“交换位置”操作，且仅在勾选 2 张凭证时可执行；交换后保留原凭证号，仅互换两张凭证的日期、状态与分录等内容。
- 交换逻辑不再受凭证未审核、已审核、已记账状态限制，但限制为同一账套、同一期间的两张凭证，避免跨期间交换造成数据失配。
- 新增 `voucherSwap` 交换计划辅助模块与单元测试，并将交换能力通过 IPC 和 `window.api.voucher.swapPositions` 暴露给凭证管理页。

2026-03-06

- 优化期末损益结转范围：默认仍仅统计当前期间已记账且非结转凭证；新增“未记账凭证”勾选项后，可扩大到当前期间全部状态的非结转凭证。
- 打通损益结转预览与执行参数：`plCarryForward.preview/execute` 统一支持 `includeUnpostedVouchers` 开关，并补充服务层测试覆盖默认口径与扩大口径两条路径。
- 同步更新损益结转页面与规则设置页文案，明确默认口径、扩大范围后的行为差异，并修订 `PROJECT_SPEC.md` 中的口径说明。

2026-03-06

- 优化凭证录入删除恢复逻辑：当当前查看的凭证已被删除时，凭证录入页会自动切回新建状态，并重新获取当前期间下一个凭证号，避免结转凭证删除后页面停留在失效只读态。
- 优化凭证分录交互：新增下一行自动继承上一行摘要，支持在新建行回车追加或直接聚焦下一行时自动带入摘要，减少重复录入。
- 调整凭证保存清洗逻辑：前端与 IPC 保存入口统一忽略无金额或金额为 0 的分录行；同时将分录输入焦点高亮改为内嵌描边，修复上侧和左侧高亮被裁切的问题。

2026-03-06

- 修复凭证录入残留编辑请求问题：当已打开的结转凭证被删除后，当前页签上的旧 `editVoucherId/editRequestKey` 请求会被标记为失效，避免再次把页面拉回已删除凭证的加载态。
- 调整新建凭证恢复逻辑：点击“新建”或检测到当前凭证已删除时，会立即清除 `loadingVoucher` 锁定状态，再重置为空白凭证并刷新下一个凭证号，恢复输入框可点击、可录入。

2026-03-06

- 扩展结账流程：`period:getStatus` 新增未审核/已审核未记账凭证摘要，`period:close` 返回下一会计期间，新增 `period:reopen` 反结账能力，并在 `PROJECT_SPEC.md` 中同步记录闭账冻结与反结账规则。
- 增加闭账冻结约束：当前期间已结账时，凭证新增、修改和取号统一被后端拦截；前端凭证录入页与凭证管理页同步显示只读提示，未审核/未记账凭证仍保留删除路径。
- 优化期间切换体验：结账页改为并列悬浮的“反结账/执行结账”按钮，结账成功后可选择是否立即进入下一会计期间；同时补充 `periodState` 服务测试并完成全量 `vitest` 与 `typecheck` 验证。

2026-03-06

- 调整凭证删除模型：`delete` 改为软删除并新增 `已删除` 状态，凭证表补充 `deleted_from_status` 用于保存删除前状态；同时为旧库增加 `vouchers` 表兼容迁移。
- 凭证管理新增状态分类标签：支持“全部、未审核、已审核、已记账、已删除”五个标签，并按标签切换上方功能按钮组合；删除标签下新增“撤回删除”“彻底删除”操作。
- 打通恢复与彻底删除链路：后端新增 `restoreDelete/purgeDelete` 批量动作，前端在执行撤回删除和彻底删除前增加确认交互，并同步扩展 preload 类型、查询页状态枚举与相关测试。

2026-03-06

- 调整凭证管理布局：将“全部、未审核、已审核、已记账、已删除”状态标签从刷新/修改等操作按钮区拆出，吸附到凭证列表交互框顶部，保持标签与列表容器同层展示。
- 扩展凭证列表字段：在“状态”和“借方合计”之间新增“摘要”列，列表查询返回并展示每张凭证首条分录摘要，空摘要时显示 `-`。
- 同步完成类型与自动化验证：补充 `voucher:list` 返回类型中的 `first_summary` 字段，并再次通过全量 `typecheck` 与 `vitest` 验证。

2026-03-06

- 扩展工作标签交互：主功能界面的工作标签除点击右侧 `X` 外，现支持双击鼠标左键直接关闭，并复用现有 `closeTab` 流程保持关闭后的激活标签切换逻辑一致。
- 为标签补充提示文案：工作标签悬停时显示“该标签双击可关闭”，降低新交互发现成本。
- 完成 `typecheck` 与全量 `vitest` 验证，确保标签切换与工作区渲染链路未受本次改动影响。

2026-03-08

- 重写 `prds/prd.md` 与 `prds/PROJECT_SPEC.md`，将产品定位收敛为“代理记账企业为委托单位记账使用的单机版财务软件”，仅支持 `enterprise` 与 `npo`，并新增 `prds/合规整改计划.md` 作为专项整改台账。
- 新增合规模块与数据底座：补充操作日志、账套删除前置校验、电子凭证、归档导出、备份恢复等服务与 IPC，并为 `window.api` / preload 类型补齐 `auditLog`、`backup`、`archive`、`eVoucher` 接口。
- 调整核心账务控制：已记账凭证默认不可逆，`unbookkeep` 收敛为管理员“紧急逆转”，强制填写原因与审批标记并写入操作日志；同时为已记账元数据、紧急逆转轨迹和电子凭证关联补充数据库字段与表结构。
- 新增电子凭证、备份包、归档包、操作日志等单元测试，并完成本轮回归验证：`npm run typecheck`、`npm test`。

2026-03-08

- 更新 `AGENTS.md`：将产品定位、支持账套范围、已记账不可逆、管理员紧急逆转、账套删除前置校验、合规模块与 IPC/文档同步要求固化为代理开发约束。
- 补充合规测试约束：要求后续涉及合规整改时优先覆盖反记账留痕、电子凭证链路、归档导出与备份恢复等关键场景，并至少执行 `npm run typecheck` 与 `npm test`。

2026-03-09

- 完成“账套设置-期末损益结转设置”正式开发：`PLCarryForward` 页面从只读预览改为可维护规则的设置页，支持加载末级损益科目、选择结转目标、批量应用与恢复默认建议。
- 新增 `plCarryForward:saveRules` IPC 与 preload 类型暴露，打通 `src/main`、`src/preload`、`src/renderer` 三层接口，允许账套设置权限用户保存整套损益结转规则。
- 收紧损益结转规则校验：主进程要求末级损益科目必须完整配置结转目标，且目标必须是允许范围内的末级“本年利润/净资产”科目；规则缺失或失效时，预览与执行损益结转会直接报错阻断。
- 补充 `plCarryForward` 服务层测试，覆盖规则保存替换、缺失规则拦截、预览前完整性校验，并完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 为“账套设置-期末损益结转设置”补充显式的个别科目自定义入口：每行新增“自定义设置”按钮，点击后可在弹窗内单独修改该末级损益科目的结转目标。
- 保留批量应用、恢复默认建议与统一保存链路不变；个别科目在弹窗中调整后，会先写入页面草稿，再通过“保存规则”统一提交到账套。
- 完成回归验证：`npm run typecheck`、`npm test`。

2026-03-09

- 按页面交互要求调整“期末损益结转设置”入口：新增顶部主按钮“自定义编辑”，位置放在“保存规则”右侧，作为个别科目规则维护的显式入口。
- 将自定义维护改为独立交互框：点击“自定义编辑”后进入单独弹窗，在弹窗内可对每个末级损益科目逐条编辑期末结转对应规则，确认后再应用回主页面草稿。
- 保持原有后端校验、批量应用与统一保存流程不变，并完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 开发报表输出基础能力：新增 `report_snapshots` 表、`reporting:generate/list/getDetail` IPC 以及 preload 类型，支持资产负债表、利润表/业务活动表、现金流量表生成并保存报表快照。
- 为“报表输出”交互框新增“报表查询”功能按钮；新增报表查询页，提供已生成报表清单、单击选中 + 查询按钮、双击行查看详情，以及按报表类型、会计期间的单选/多选筛选。
- 新增报表快照服务与前端筛选工具测试，并完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 扩展报表快照生成口径：资产负债表改为按月份生成并以所选月份最后一天作为截至时点；利润表、业务活动表、现金流量表改为按起始月份到结束月份生成，实际取数范围为起始月份 1 日至结束月份最后 1 日，支持跨年范围。
- 为报表生成功能新增“未记账凭证”勾选项，勾选后将未删除的未审核、已审核、已记账凭证一并纳入报表计算；同时将报表快照改为完整模板列示，无发生数行也保留显示。
- 为报表查询页新增报表删除能力与删除确认对话框；双击报表或点击“查询详情”后，改为在悬浮交互框中显示完整报表。
- 新增/更新 `reporting` 服务测试，覆盖月份边界、跨年区间、未记账口径和快照删除，并完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 新建系统级技能 `C:\\Users\\11441\\.codex\\skills\\qiye-cas-report-templates`，将企业会计准则四大报表模板整理为可触发的正式 SKILL。
- 补齐技能资源：新增现行依据说明、输出/校验规则、四大报表 canonical JSON 模板，并修正原草稿中“合同负债误列入资产端”等列报错误。
- 新增 `agents/openai.yaml` 以补全系统级技能展示与默认调用元数据；按要求删除系统级原草稿目录 `C:\\Users\\11441\\.codex\\skills\\qiye baobiao`，仅保留成品技能。
- 按项目约束执行验证：`npm test` 通过（17 个测试文件 / 85 个用例）；`npm run typecheck` 未通过，现存 `src/main/services/reporting.ts` 第 786、800、814 行触发 TS2367，属于仓库已有问题，未在本次任务内改动。

2026-03-09

- 读取 `.codex/skills/ngo2026zhunze/SKILL-NGO baobiao.md` 中的系统级民非资产负债表模板，并将 `reporting` 服务中的民非资产负债表输出切换为对应模板行次与项目名称。
- 为民非资产负债表补充“年初数 / 期末数”双列快照数据结构，新增行次、模板列配置与受托代理资产/负债、净资产合计、负债和净资产总计等模板行映射。
- 更新报表详情渲染组件，支持按模板列展示民非系统级报表行，并补充民非模板测试覆盖。

2026-03-09

- 为报表生成新增重复校验：同账套下如已存在同报表类型、同会计期间范围的报表快照，则阻止再次生成，并提示需先删除原报表。
- 新增 `reporting:export` IPC 与前端显式“导出 Excel / 导出 PDF”按钮，在报表生成页、报表查询页和报表详情悬浮框中均可触发导出；导出通过另存为方式保存到用户指定路径。
- 更新报表服务测试，覆盖重复生成拦截与导出 HTML 内容，并完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 引入 `exceljs`、`pdfkit` 与 `@types/pdfkit`，为报表导出补齐 Excel 与 PDF 两种文件格式；其中 PDF 通过报表 HTML 模板走浏览器打印内核输出，保证中文和表格样式更稳定。
- 更新报表服务测试，新增 Excel/PDF 文件生成校验；前端将导出入口改为显式的“导出 Excel / 导出 PDF”按钮，报表查询详情悬浮框中也同步提供两种导出按钮。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 根据《民间非营利组织会计制度》会民非 01/02/03 表制式，重构民非资产负债表、业务活动表、现金流量表的报表数据模型，改为官方表格结构并在查询、生成预览、Excel/PDF 导出三处复用。
- 业务活动表切换为“本月数（非限定性/限定性/合计）+ 本年累计数（非限定性/限定性/合计）”列结构；现金流量表切换为“本年金额 / 上年金额”列结构；民非资产负债表保持官方双侧并列表格结构。
- 报表生成页改为在生成后弹出独立的大尺寸交互框展示报表，扩大可视面积，并完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 将“紧急逆转”恢复为用户可见名称“反记账”，并新增独立 `unbookkeep` 权限位；管理员可在账号管理中为非管理员账号勾选/保存该权限，后台仍强制记录原因、审批标记与操作日志。
- 修复凭证管理页点击反记账时报 `prompt() is not supported` 的问题，改为页面内悬浮表单收集反记账原因与审批标记后再提交。
- 重构民非资产负债表（会民非01表）汇总逻辑，按流动资产、非流动资产、资产总计、负债合计、净资产合计、负债和净资产总计逐项计算，修正重复累计导致的合计错误。
- 修正损益结转凭证 `结-000X` 编号规则，改为按 `voucher_word='结'` 独立从 `0001` 起算，不再沿用当期普通凭证最大号。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-09

- 将系统级技能草稿 `ngo baobiao` 迁移并固化为成品技能 `ngo-report-templates`，补齐标准 frontmatter、`agents/openai.yaml` 与按需加载的 `references/` 结构。
- 基于《民间非营利组织会计制度》（财会〔2024〕25号）补全 `会民非01` 资产负债表、`会民非02` 业务活动表、`会民非03` 现金流量表模板，并补充 2026 版口径、旧版差异与官方来源说明。
- 完成系统级技能校验：`quick_validate.py C:\\Users\\11441\\.codex\\skills\\ngo-report-templates`。

2026-03-10

- 优化会计科目新增流程：当新增利润类子级科目且父级已挂接期末损益结转目标时，系统自动为新科目补齐同目标的结转规则。
- 在 `accountSetup` 服务测试中补充利润类子科目继承结转目标的用例，并扩展假数据库以覆盖 `pl_carry_forward_rules` 读写行为。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 修复企业账套新增同父级损益明细科目后仍提示“未配置结转目标”的问题：当父级已因存在子级而不再保留直接规则时，科目创建会改为读取父级现有后代规则；若后代规则唯一，则自动沿用该结转目标。
- 为损益结转服务增加旧数据自愈逻辑：在规则列表/结转预览前，自动补齐这类“同父已有唯一后代规则、但新叶子缺规则”的历史账套数据，避免现有账套继续报缺失错误。
- 为 `accountSetup` 与 `plCarryForward` 增加回归测试，覆盖 `6001` 仅保留 `600101` 规则时新增 `600102` 自动继承/自动补齐结转目标的场景。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 核查并修复企业账套资产负债表权益汇总逻辑：企业资产负债表此前手工汇总“所有者权益合计”时未复用统一权益状态计算，导致 `4102` 等权益科目可能被漏算。
- 将企业资产负债表中的权益行与总计改为直接读取 `buildEnterpriseEquityState` 的结果，保证与权益变动表和统一权益状态口径一致。
- 为报表服务补充回归测试，覆盖企业资产负债表在存在“一般风险准备”余额时资产总计与负债及所有者权益总计保持一致的场景。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 通过读取真实运行库定位到企业资产负债表 2026-01 取数为 0 的根因：默认账套 `start_period=2026-03`、`current_period=2026-01` 倒挂，导致报表生成按账套启用期间起算时形成空取数区间。
- 调整报表服务的企业资产负债表取数起点，改为使用 `start_period`、`current_period` 与目标期间中的最早有效期间；同时在账套切回更早会计期间时自动同步下调 `start_period`，并在加载账套列表时按当前期间/期间表/凭证/期初中的最早期间自动修正历史账套的启用年月，避免后续再生成同类坏数据。
- 为报表服务补充回归测试，覆盖“账套起始期间晚于当前/目标期间但 2026-01 已有记账凭证时，资产负债表仍能取到 1 月数据”的场景。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 新增账簿查询试点能力：主进程增加 `bookQuery:listSubjectBalances` 与 `bookQuery:getDetailLedger`，当前仅对 `npo` 账套开放，先支持科目余额表与科目明细账两项查询。
- 新增民非账套账簿取数服务与测试：覆盖科目余额表父级聚合、末级科目明细账期初余额与逐笔滚动余额，并阻断企业账套与非末级科目明细账查询。
- 新增 `SubjectBalance`、`DetailLedger` 页面并接入工作区；科目余额表支持右键“查询明细账”，科目明细账支持右键“查询凭证”。
- 扩展 `VoucherQuery` 支持预置参数与精确 `voucherId` 查询，补齐从明细账跳转到凭证查询的交叉查询链路。
- 同步更新 `src/main`、`src/preload`、`src/renderer` 三层接口与组件映射，完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 修正民非账套基础科目分类：默认模板与旧账套兼容迁移统一改为 `asset/liability/net_assets/income/expense`，不再沿用 `equity/profit_loss` 作为民非存储分类。
- 为 `subjects` 表补充分类兼容迁移逻辑：旧版民非账套中的 `equity` 自动迁移为 `net_assets`，`profit_loss` 按余额方向自动迁移为 `income` 或 `expense`。
- 同步修正民非默认账套种子与期末结转规则：民非结转来源改为收入/费用类，结转目标限定为净资产类；并为该规则补充种子与回归测试。
- 同步修正民非账套相关模块口径：报表模块、期末结转模块、会计科目设置页、凭证录入科目树均改按新分类工作，并保留对旧分类值的兼容识别，避免迁移窗口内出现功能断裂。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 调整民非账套“科目余额表”查询口径：由单一会计期间改为任意开始日期/结束日期区间查询，期初余额按区间起始日前滚计算，本期发生额按所选区间统计，期末余额按区间结束日计算。
- 为科目余额表新增两个查询选项：`未记账凭证` 与 `显示无余额科目`；默认只统计已记账凭证，且默认隐藏期初/本期/期末均为 0 的科目。
- 同步扩展账簿服务与类型声明：`bookQuery:listSubjectBalances`、`bookQuery:getDetailLedger` 支持区间日期与 `includeUnpostedVouchers`，并补充回归测试覆盖区间口径、未记账口径、零余额过滤与明细账联动。
- 修正科目余额表交互：移除双击直接进入明细账，改为右键弹出账簿导航菜单；当前开放“查询明细账”，并预留禁用态“查询总账”“查询辅助账”入口，便于后续扩展。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 调整科目余额表展示结构：移除“方向”列，查询条件区改为两行布局，第一行放置日期范围、科目名称、查询与“全屏查看”按钮，第二行放置“未记账凭证”“显示无余额科目”选项。
- 为科目余额表和科目明细账新增“全屏查看”独立交互框，点击后会先按当前条件执行查询，再在独立交互框中以更大面积显示结果表格。
- 修复账簿页面右键操作：放弃当前不稳定的上下文菜单触发方式，改为手工右键菜单状态控制；科目余额表行右键现可稳定弹出“查询明细账/查询总账/查询辅助账”，科目明细账业务行右键现可稳定弹出“查询凭证”。
- 调整科目明细账查询区：移除期间选择器，仅保留日期范围、科目选择、查询按钮、全屏查看按钮，并将“未记账凭证”选项下沉到第二行。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 继续调整账簿查询交互：科目余额表将“科目名称输入框”改为起止科目范围选择器，左侧选择起点、右侧选择终点，两个都为空时等价于全部科目。
- 科目余额表查询条件布局继续收敛为两行：第一行仅保留日期范围与起止科目范围，第二行顺序改为“查询”“全屏查看”“未记账凭证”“显示无余额科目”。
- 科目明细账查询条件布局同步收敛：删除期间选择器，第一行为日期范围、科目选择、查询、全屏查看，第二行仅保留“未记账凭证”。
- 为科目余额表和科目明细账的常规结果区与全屏交互框统一补齐纵向滚动能力，并确保两种展示状态下都支持右键功能菜单。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 修复账簿全屏查看交互框中的右键菜单层级问题：科目余额表与科目明细账的手工右键菜单统一改为通过 `createPortal(..., document.body)` 渲染，避免被全屏弹窗遮挡导致菜单不显示。
- 调整科目余额表查询条件布局：第一行改为日期范围、查询、全屏查看、未记账凭证、显示无余额科目，第二行保留起止科目范围选择器。
- 保持常规视图与全屏视图的右键菜单行为一致，未改动后端取数口径与账簿交叉查询链路。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 修复账簿全屏查看窗口中的跨页跳转失效问题：由于工作区切换页签时旧页签仅隐藏不卸载，而全屏弹窗走 `Dialog.Portal` 挂在 `body` 上，导致点击右键菜单后目标页签已切换但旧弹窗仍停留在界面上。
- 调整科目余额表与科目明细账的右键菜单跳转逻辑：执行“查询明细账”“查询凭证”前先关闭当前全屏弹窗并清理右键菜单，再切换到目标账表页签。
- 本次仅修正前端交互状态切换，不改动账簿取数、凭证查询参数与 IPC 接口。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 继续针对“科目余额表 -> 全屏查询 -> 选择科目 -> 右键 -> 查询明细账”链路排查，确认原实现还存在两处拦截：菜单按钮点击时序不稳，以及明细账服务/前端选择器只允许末级科目，导致父级科目右键后不会真正打开明细账标签。
- 调整科目余额表右键菜单触发方式：`查询明细账` 改为在 `onMouseDown` 阶段先执行跳转，规避菜单自身的全局关闭事件吞掉点击。
- 扩展明细账查询口径：`bookQuery:getDetailLedger` 改为支持父级科目按下级分录汇总查询，`DetailLedger` 页面科目选择器同步放开为全科目列表，保证从余额表选中的科目都能落到明细账页。
- 新增回归测试覆盖父级科目明细账聚合查询，并完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 继续针对“科目余额表全屏查询弹窗内右键菜单可见但不可点击”排查，确认根因是菜单在全屏状态下仍 portal 到 `document.body`，而 `Radix Dialog` 会禁用 `Dialog.Content` 外部元素的指针事件，因此出现“能看到菜单、没有 hover 效果、点击无响应”。
- 调整科目余额表右键菜单挂载策略：普通视图仍 portal 到 `document.body`，全屏弹窗视图改为直接渲染在 `Dialog.Content` 内部，恢复菜单 hover 高亮与点击跳转能力。
- 同步保留 `查询明细账` 的显式 hover 交互样式与阻止默认冒泡处理，避免菜单点击被其他全局关闭逻辑抢先吞掉。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 修正账簿全屏右键菜单文案：科目余额表与科目明细账当前启用的右键菜单项统一改为正常中文显示，不再出现乱码。
- 扩展全屏跳转状态传递：从科目余额表全屏右键进入科目明细账时，新增 `presetOpenPreview` 参数并在明细账页自动按同样的全屏方式打开。
- 同步将科目明细账的全屏右键菜单也改为在 `Dialog.Content` 内部渲染，避免后续“全屏可见但不可点”的同类问题重复出现。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 优化“全屏A -> 全屏B”导航回退：从科目余额表全屏进入科目明细账全屏时，额外记录来源页签及查询参数，作为 `returnTabOnPreviewClose` 返回目标。
- 调整科目明细账全屏关闭逻辑：当该页由全屏余额表钻取打开时，关闭明细账全屏不再停留在明细账页，而是自动关闭当前明细账标签并恢复来源的科目余额表全屏。
- 同步保留来源页的日期范围、起止科目范围、未记账/零余额口径，并在返回时重新按原条件打开全屏余额表。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 继续优化“全屏A -> 全屏B”回退体验：关闭全屏明细账时，仍会切回来源的科目余额表全屏，但不再删除明细账工作标签。
- 明细账标签会以普通工作标签的形式保留在工作区中，便于后续再次切回查看或继续操作。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-10

- 调整科目明细账交叉查询凭证的跳转目标：右键“查询凭证”不再先进入凭证查询模块，而是直接打开指定凭证的凭证录入页面。
- 明细账页改为向 `VoucherEntry` 传递 `editVoucherId/editRequestKey`，复用现有凭证查看/编辑加载链路。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 新增序时账模块：主进程补充 `bookQuery:getJournal` 查询能力，支持日期范围、科目范围、未记账凭证口径，并按凭证日期/字号/分录顺序返回序时账明细。
- 新增 `Journal` 页面并接入工作区，查询区沿用账簿模块统一结构，支持日期范围、起止科目范围、未记账凭证勾选、查询和全屏查看。
- 序时账结果区与全屏视图均支持右键菜单，当前开放“查询凭证”，直接跳转到指定凭证录入页查看；其余账簿联动本轮未展开。
- 同步更新 `src/main`、`src/preload`、`src/renderer` 三层接口与类型声明，并在 `PROJECT_SPEC` 中补记 `bookQuery:getJournal`。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 新增总账余额表模块：主进程补充 `bookQuery:getTrialBalance`，当前复用科目余额取数口径，为总账余额表保留独立 IPC 入口。
- 新增 `TrialBalance` 页面并接入工作区，支持日期范围、科目范围、未记账凭证、查询和全屏查询，整体容器逻辑与科目余额表/科目明细账保持一致。
- 总账余额表结果行支持右键菜单：当前开放“查询明细账”，并按科目是否配置辅助核算控制“查询辅助账”是否可点；辅助账入口暂跳转到现有占位页，便于后续扩展真实模块。
- 从总账余额表全屏进入科目明细账时，继续沿用“全屏 A -> 全屏 B”钻取模式，并在 `PROJECT_SPEC` 中补记 `bookQuery:getTrialBalance`。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 按要求取消总账余额表模块，删除 `TrialBalance` 页面与工作区按钮入口，并撤销对应 `bookQuery:getTrialBalance` IPC、preload 暴露、类型声明和服务测试。
- 同步修正 `PROJECT_SPEC` 与 `prd`，移除总账余额表相关 API 和阶段范围描述，避免文档继续保留已取消模块。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 新增辅助余额表与辅助明细账模块：主进程补充 `bookQuery:getAuxiliaryBalances`、`bookQuery:getAuxiliaryDetail`，支持日期范围、科目范围、未记账凭证和辅助项目明细查询。
- 新增 `AuxiliaryBalance`、`AuxiliaryDetail` 页面并接入工作区，整体交互结构对齐科目余额表/科目明细账，支持查询、全屏查看、右键菜单和从辅助余额表钻取辅助明细账。
- 打通科目余额表到辅助余额表的交叉查询入口，并保持全屏 A -> 全屏 B 的账簿钻取模式；辅助明细账右键可直接跳转凭证录入界面。
- 同步更新 `src/main`、`src/preload`、`src/renderer` 三层接口与类型声明，重写账簿页公共工具与菜单标题，修正辅助账相关中文文案显示。
- 同步补记 `PROJECT_SPEC` 当前账簿试点现状，并完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 修复辅助明细账查询报错：`bookQuery:getAuxiliaryDetail` 使用的辅助分录 SQL 中，`voucher_word = '记'` 条件字符串损坏，导致 SQLite 报 `unrecognized token`。
- 将 `src/main/services/bookQuery.ts` 中辅助账查询排序 SQL 修正为合法字面量，恢复辅助明细账查询执行。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 修复辅助余额表/辅助明细账对历史凭证的兼容问题：当 `voucher_entries.auxiliary_item_id` 为空，但科目仅绑定唯一“自定义辅助项”时，账簿查询会按该唯一自定义辅助项自动归集。
- 调整 `bookQuery:getAuxiliaryDetail`：辅助项目元数据不再依赖已有分录反查，避免无直接辅助分录时误报 `auxiliary detail not found`。
- 补充服务测试，覆盖“直接辅助分录”与“唯一自定义辅助项推断”两条查询路径。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 调整账簿查询模块所有子功能的默认日期范围，统一改为“本年 1 月 1 日至今天”，涉及科目余额表、科目明细账、序时账、辅助余额表、辅助明细账。
- 新增账簿查询公共日期范围工具，并补充对应单元测试。
- 调整账簿查询模块子功能按钮顺序，当前排列为：第一行“科目余额表 / 科目明细账 / 序时账 / 辅助余额表”，第二行“辅助明细账”。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 在已完成的民非账套账簿查询基础上，放开企业账套的账簿查询支持，覆盖科目余额表、科目明细账、序时账、辅助余额表、辅助明细账五个子功能。
- 移除 `bookQuery` 服务层与前端账簿页中“仅民非可用”的试点拦截，统一为企业账套和民非账套共用查询链路。
- 补充企业账套账簿查询测试数据，覆盖科目余额表、科目明细账、序时账、辅助余额表、辅助明细账的查询场景，并同步更新 `PROJECT_SPEC`。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 调整辅助明细账筛选区布局：第一行改为日期范围、查询、全屏查看；第二行改为科目、辅助项目、未记账凭证。
- 保持辅助明细账查询参数和右键查询凭证链路不变，仅整理筛选交互排布并统一页面中文文案。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 新增账簿查询通用导出能力：科目余额表、科目明细账、序时账、辅助余额表、辅助明细账均支持按当前筛选结果导出 `Excel` 和 `PDF`。
- 主进程新增通用账簿导出器与 `bookQuery:export` IPC，支持另存为保存、文件类型过滤、导出操作日志写入，以及“上一次保存目录”记忆。
- 渲染层 5 张账簿页统一增加 `导出 Excel`、`导出 PDF` 入口，并按各自表头/行数据构建导出 payload。
- 新增 `bookExport` 服务测试，覆盖 Excel/PDF 落盘；同步更新 `PROJECT_SPEC`。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 新增账簿筛选区公共组件 `ScaledFilterRow`，将账簿查询各子模块筛选容器统一为“三行布局”：第一行日期选择器和功能按钮，第二行科目相关选择器，第三行勾选条件。
- 当筛选行总宽度超过容器宽度时，统一按比例缩放整行，避免控件换行挤压，已应用到科目余额表、科目明细账、序时账、辅助余额表、辅助明细账。
- 修复账簿 PDF 导出中文乱码：导出器优先加载系统中文字体，并在不可用时安全回退；同时补充明细账导出副标题，显示当前科目代码和中文名称。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 优化账簿导出版式：PDF 导出按表头和内容宽度自动计算列宽，并在超出页面宽度时同步缩放整体表格与字体大小；Excel 导出对所有列按内容执行 autofit。
- 调整账簿导出头部信息：标题下一行居中显示账套名称；日期或日期范围单独一行右对齐；科目明细账和辅助明细账在有科目时改为“科目左对齐、日期右对齐”的同一行。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 调整 PDF 导出表格宽度分配：当内容较短未撑满页面时，按比例放大各列宽度以铺满可用页面宽度；仅在内容超宽时才缩小字号和行高。
- 同步完善导出头部元数据结构，账簿导出支持分开传递账套名称、科目名称、日期范围，便于 Excel/PDF 按统一版式排版。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 会计准则设置页新增“自定义一级科目模板”能力：按 `enterprise` / `npo` 两类账套分别维护自定义一级科目模板，并在页面中展示模板数量、最近导入时间和模板预览。
- 新增独立弹窗式批量导入交互，支持下载 Excel 导入模板、导入模板文件、清空当前准则模板；模板文件采用“导入说明 + 一级科目模板”双工作表结构。
- 主进程新增一级科目模板服务与 `settings:getSubjectTemplate/importSubjectTemplate/downloadSubjectTemplate/clearSubjectTemplate` IPC，同步更新 preload 暴露与类型声明，并为模板导入/清空写入操作日志。
- 新建账套与空账套准则重建时，会在标准科目种子后自动附加对应准则的自定义一级科目模板；补充模板服务测试，覆盖系统科目冲突校验、民非结转目标校验、模板生成/解析和账套应用。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 按确认后的模板格式继续优化导出模板：新增“填写示例”工作表，并为企业/民非两类账套分别预置 3 条示例一级科目，便于使用者理解填写方式。
- 更新导入说明页文案，明确示例页仅供参考，正式导入数据应填写到“一级科目模板”工作表。
- 补充模板导出测试，校验下载模板包含示例工作表和示例行数据。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 将模板弹窗中的“导入模板”按钮改名为“模板下载”，并把 Excel 模板中的“科目类别”改为中文下拉选项；主进程导入解析同步支持中文类别到系统英文分类码的映射。
- 一级科目模板维护权限收紧为仅 `admin` 可写：模板下载、批量导入、手动保存、清空模板四类操作均改为管理员校验；普通用户仅可只读查看。
- 模板弹窗新增“手动维护”表格，支持在弹窗内新增/删除/编辑一级科目模板条目并整体保存；表格容器预留横向、纵向滚动区域以应对内容过多场景。
- 同步新增 `settings:saveSubjectTemplate` IPC，更新 `PROJECT_SPEC`、preload 暴露与类型声明，并保持模板导入/手动维护共用同一套后端校验逻辑。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 继续细化一级科目模板手动维护弹窗：左侧新增“已有一级科目参考”只读清单，便于在维护自定义模板时对照系统现有一级科目微调。
- 将“新增一行”改为“新增科目”，取消排序号字段，模板下载、导入解析、手动保存统一改为按科目代码自动排序。
- 将手动维护表中的“现金”列改为“现金流量科目”，并把结转目标改为下拉框选择；同时新增 `settings:getSubjectTemplateReference` IPC 为前端提供准则下的内置一级科目参考数据。
- 同步更新模板服务测试，覆盖已有一级科目参考读取、模板表头去除排序号、中文类别下拉及导入解析。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 调整一级科目模板入口文案：主页面统一改为“模板维护”按钮，弹窗内集中承载模板下载、批量导入、清空模板、新增科目、保存维护五类操作。
- 取消弹窗中的“已有一级科目参考”独立列表，改为单一业务交互框；弹窗关闭后再次打开时，保留上次关闭时的未保存维护状态。
- 优化模板维护表格列宽：按内容采用更紧凑的 autofit 宽度策略，减少固定宽列造成的横向浪费，同时保留横向/纵向滚动能力。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 继续调整模板维护弹窗滚动行为：取消手动维护区内部纵向冻结滚动，改为由整个弹窗承担纵向滚动，使顶部说明容器在下拉时自然滚出视口，不再固定顶置。
- 将“手动维护”容器标题改为“手动维护记录”，并保留“当前弹出框显示既有科目，可直接微调”的说明。
- 主页面“模板维护”入口保持独立按钮，弹窗关闭后不再清空当前准则上下文，从而真正保留上次关闭时的维护状态。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-11

- 在模板维护弹窗中新增“当前模板科目”容器，并放置在“手动维护记录”容器下方；该容器用于列示当前模板下已存在的一级科目，并支持直接手动修改维护。
- 将“手动维护记录”容器改为展示当前未保存的新增、修改、删除摘要，和“当前模板科目”列表型维护区形成上下分层。
- 新增科目后会立即进入弹窗中的“当前模板科目”维护列表，保存后继续保留在该容器中作为模板正式条目。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 继续收敛模板维护弹窗分工：将“当前模板科目”容器明确为仅列示当前模板下已存在的一级科目列表，并支持在列表中直接手动修改维护。
- 将“手动维护记录”容器进一步调整为“待保存变更摘要 + 待新增科目”区域；未保存的新科目先停留在该区域，保存后再进入“当前模板科目”容器。
- 保持“新增科目 -> 填写 -> 保存 -> 进入当前模板科目列表”的流程闭环，并完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 修正“当前模板科目”容器取数口径：由仅显示“已保存自定义条目”改为显示“当前准则既有一级科目基线 + 已保存模板覆盖/新增”的合并视图，解决空模板时容器看不到现有一级科目列表的问题。
- 模板保存现已支持对内置一级科目做覆盖维护，模板应用到账套时会优先更新同编码的既有一级科目，再补充新增自定义一级科目。
- 补充模板服务测试，覆盖“允许保存内置一级科目覆盖项”的场景，并完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 在模板维护弹窗顶部操作区新增“关闭”按钮，放置在“清空模板”右侧，并移除弹窗底部重复的关闭按钮，统一收口顶部操作入口。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 按交互要求恢复模板维护弹窗底部“关闭”按钮，与顶部“关闭”按钮并存，两者行为保持一致，仅用于提升操作便利性。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 在“会计准则设置”页顶栏新增“自定义模板”按钮入口，放置于“刷新模板”右侧；点击后进入独立交互容器，专门维护“自行添加的一级科目模板”。
- 新增独立 `AccountingStandardCustomTemplateDialog`，支持模板名称录入、模板说明录入、模板下载、批量导入、新增科目、保存、清空模板、关闭；其中“清空模板”语义为一键删除自定义新增科目。
- 主进程新增 `settings:parseSubjectTemplateImport` IPC，用于“选择导入文件并解析但不立即保存”；前端基于该结果与现有模板数据做合并后再保存，避免覆盖“模板维护”产生的基线覆盖项。
- 一级科目模板存储结构新增 `templateDescription` 元数据字段，并同步更新 preload 暴露、类型声明与项目规格文档。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 修正独立“自定义模板”的业务边界：其模板数据不再写回系统预设 `enterprise/npo` 模板存储，而是改为独立模板目录单独保存、单独列示、单独维护。
- 允许独立自定义模板使用与系统预设模板相同的一级科目编码；编码唯一性仅在同一自定义模板内部校验，不再与系统预设模板做冲突拦截。
- 会计准则设置页新增“自定义模板列表”区域，将保存后的独立模板列示在系统预设模板区下方，并提供“独立维护”入口。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 补齐一级科目批量导入模板的数据验证范围：`余额方向`、`现金流量科目`、`是否启用`、`期末结转目标科目` 与 `科目类别` 一样，均通过隐藏“选项数据”工作表提供下拉约束。
- 更新模板服务测试，改为校验隐藏选项表、下拉公式引用与选项内容，确保模板下载文件实际包含上述 5 列的受限选项。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 调整一级科目导入模板中“期末结转目标科目”的下拉显示值，改为展示“科目代码 + 科目名称”；导入解析时后端自动转译回内部科目代码。
- 修正标准模板与独立自定义模板的批量导入逻辑：改为按科目代码与当前草稿/已保存条目合并，同码覆盖、异码追加，避免批量导入整体覆盖原有手动新增科目。
- 新增前端合并工具测试与模板服务测试，覆盖“导入不丢手动条目”和“结转目标下拉显示名称并可正确转译”的场景。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 将自定义模板列表卡片中的“独立维护”按钮统一更名为“模板维护”，与系统预设模板的入口命名保持一致。
- 优化两个“模板维护”弹窗中当前已有科目列表的下拉框宽度计算逻辑：改为按当前值与候选项最长文本自动扩展，避免类别、借贷方向、现金流量、是否启用、结转目标等下拉内容显示不全。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 继续优化模板维护弹窗的下拉宽度策略：为短文本下拉（方向、启用等）补充更大的最小宽度和箭头缓冲，避免当前已有科目列表中状态值被裁切。
- 新增“删除模板”能力，仅允许删除独立自定义模板；系统预置模板仍只支持维护和清空，不支持删除模板实体。
- 主进程同步新增独立自定义模板删除服务、IPC、preload 暴露与类型声明，并补充服务层测试验证“删除自定义模板不影响系统预置模板”。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 新增账套级访问控制模型：数据库补充 `user_ledger_permissions` 账套授权关系表，并在旧库升级时为历史普通用户自动回填“已有全部账套可访问”授权，避免升级后账套列表被意外清空。
- 登录与账号管理现支持维护账套授权：`auth:getUsers/createUser/updateUser` 同步返回和保存 `ledgerIds`，账号管理页新增“账套权限”勾选区，可按用户分配可操作账套。
- 主进程补齐账套访问校验：`ledger:getAll` 按当前登录用户过滤可见账套，`admin` 默认可访问全部账套；同时为账套、科目、辅助、现金流量、期初、凭证、结账、损益结转、账簿查询、报表、备份、归档、电子凭证等核心 IPC 增加账套访问校验，拦截未授权账套操作。
- 主界面账套切换同步适配授权结果：普通用户仅会看到被分配账套，当前账套失效时会自动切换到首个可访问账套，无可访问账套时不再自动创建默认账套。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 新增个人偏好存储模型：数据库补充 `user_preferences` 表，并新增 `settings:getUserPreferences`、`settings:setUserPreferences` IPC，用于按当前登录用户保存个人默认账套与默认首页。
- 扩展“系统参数设置”第一阶段高频效率项：在原有“允许制单人与审核人为同一人”之外，新增 `默认凭证字`、`新建凭证日期策略`、`凭证管理默认状态页` 三个系统级参数。
- 新增“我的偏好”页面与右上角入口，支持维护 `默认账套` 与 `默认首页`；登录进入主界面时会优先按个人偏好选择账套并自动打开默认首页。
- 退出登录时同步清空账套状态与工作标签，避免上一个会话残留状态覆盖新的默认账套/默认首页偏好。
- 调整凭证录入和凭证管理的默认行为：新建凭证日期现支持“继承上一张凭证日期 / 固定当前期间首日”策略，凭证管理页支持读取系统默认状态标签。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 调整主工作界面顶部信息栏布局：移除会计期间选择器后方重复显示的期间文本，仅保留选择器本身。
- 将账套选择器、账套操作按钮、会计期间选择器与“我的偏好 / 当前用户退出”操作区统一排布到同一行；桌面端优先保持单行，窄屏下再回退为换行布局。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 优化账号管理页的账套权限编辑操作：将普通用户行内的“保存权限”按钮移动到“账套权限”勾选区下方，修改账套授权后可直接就地保存。
- 右侧“操作”列保留删除按钮，避免保存按钮依赖最右侧列导致在账套权限编辑场景下不易发现。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 新增统一打印模块与主进程 `print:*` IPC，采用统一 HTML 打印文档管线，支持打印任务准备、状态轮询、预览窗口打开、系统打印、打印版 PDF 导出与任务释放。
- 第一阶段接入财务报表与全部现有账簿打印：报表工作区/报表查询页支持单张与批量打印预览；科目余额表、科目明细账、序时账、辅助余额表、辅助明细账均新增“打印预览”入口，按当前筛选结果生成打印文档。
- 第二阶段同步接入记账凭证打印：凭证录入页支持打印当前已保存凭证，凭证管理页支持勾选多张凭证批量打印；支持 `single` 单张整页与 `double` A4 一页两张（上下结构）两种版式。
- 新增 `VoucherPrintDialog`，支持调整两联上下间距，并将 `voucher_print_layout`、`voucher_print_double_gap` 记入当前用户偏好，用于记住上次凭证打印设置。
- 打印预览窗口内新增“打印”“导出 PDF”“关闭”工具条；预览、系统打印与打印版 PDF 导出统一复用同一份 HTML 内容。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 调整记账凭证打印版式：无论实际分录数量多少，单张凭证打印时统一补齐到至少 4 行分录；不足部分按空白行渲染，确保每张凭证打印高度一致。
- 补充打印服务测试，覆盖“凭证打印自动补齐 4 行分录”的场景。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 继续优化记账凭证打印空白分录行：空行单元格改为按一行文字高度占位渲染，使用不可换行占位内容保证空行高度与正常单行文字行一致。
- 补充打印服务测试，覆盖“空白分录行输出文字占位符”的场景。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 调整记账凭证打印表头：新增“单位名称：账套名”信息，并与“日期”“编号”放在同一行，分别按左对齐、居中、右对齐布局显示。
- 补充打印服务测试，覆盖凭证打印表头输出单位名称及三栏对齐类名的场景。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 继续优化记账凭证打印版式：将“制单 / 审核 / 记账”与表格下边框的间距调整为与“单位名称 / 日期 / 编号”行到表格上边框相同的数值，保证上下留白一致。
- 调整凭证表格列宽：借方金额、贷方金额列各按原等分宽度缩减三分之一，释放出的宽度按 40% 分配给摘要列、60% 分配给会计科目列，并在模板中显式写入列宽。
- 补充打印服务测试，覆盖凭证打印列宽输出与统一上下间距样式变量。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 继续优化记账凭证打印金额列：为借方金额、贷方金额及合计金额增加专用金额渲染容器，默认保持单行显示。
- 在打印预览页新增金额自适应缩字逻辑：当金额数字超出一行可用宽度时，自动逐步缩小字号，确保打印预览、系统打印与打印版 PDF 中均能单行显示。
- 补充打印服务测试，覆盖金额单元格专用类名与预览页金额自适应脚本。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 放宽凭证两联打印上下间距设置范围：前端输入与校验从 `0-120` 调整为 `0-500`，并在打印预览页新增纸张溢出检测，内容超出纸张范围时提示先减小间距或调整内容后再打印/导出 PDF。
- 修复凭证修改态的现金流量校验不一致问题：`voucher:update` 现与 `voucher:save` 共用现金流量自动匹配逻辑，内部现金互转在修改金额后不再被错误强制要求分配现金流量项目。
- 修复凭证录入页交互失焦问题：进入“修改”状态及关闭科目/现金流量弹层时，会主动恢复工作区交互状态并回到最近编辑单元格，降低点击金额、摘要无响应的概率。
- 补充回归测试，覆盖凭证更新路径的现金流量自动规则与打印预览纸张溢出检测脚本。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 调整账簿打印模板：科目余额表、辅助余额表在打印时对“科目编码”“科目名称”按级次缩进，每级使用 1 个全角字符宽度缩进。
- 所有账簿打印改为统一列宽自适应：生成 `colgroup` 按内容估算宽度并撑满页面；摘要列、科目名称列启用固定行高内的自动缩字/换行适配，避免超宽时撑破页面宽度。
- 修复独立打印预览窗口中的打印与 PDF 导出登录态问题：`print:print`、`print:exportPdf`、`print:dispose` 现支持按预览窗口绑定的打印任务访问，不再因预览窗没有独立会话而报“未登录或登录态已失效”。
- 补充打印模板与凭证现金流量回归测试，覆盖列宽自适应、级次缩进和预览页脚本关键逻辑。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 继续调整账簿打印头布局：辅助明细账现支持“主标题居中 + 左侧两行信息块”版式，左侧信息块按“科目：”“辅助科目：”上下两行显示，字体大小与编制单位/会计期间信息一致。
- 统一账簿型打印头模式：科目明细账保留标题同行科目标签样式，辅助明细账切换为标题居中样式，其余账簿继续沿用仅保留编制单位/会计期间/单位的三栏信息头。
- 补充打印服务测试，覆盖辅助明细账两行信息块与账簿头部布局回归。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 调整记账凭证打印主体表格：分录区域统一补齐到 6 行，单行高度按现有样式提升约 20%，保持单张与两联版式下表格高度稳定。
- 为记账凭证的摘要列、会计科目列新增固定行高内的缩字换行模式：内容超过一行时允许换行，但不再撑高表格行高，而是通过自动缩小字号适配单元格宽高。
- 补充打印服务测试，覆盖凭证打印 6 行补齐、摘要/会计科目缩字容器与预览页自适应脚本回归。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-12

- 新增前端菜单权限过滤规则：`Sidebar` 现按当前登录用户授权渲染主模块按钮，未授权用户不再看到“系统设置”“账套设置”等受控入口。
- 新增悬浮功能面板权限过滤：`SuspendedOverlay` 现按授权过滤子功能按钮，并在受控模块无可见子项时自动关闭悬浮菜单，避免出现空白功能面板。
- 补充 `uiAccess` 回归测试，覆盖普通用户未授权隐藏、授权后显示、管理员全量可见等场景。

2026-03-13

- 完成账套备份模块增强：`backupRecovery` 现生成带 `manifest.json` 的系统级数据库快照备份包，记录账套、年度、校验值与文件大小等元数据。
- 收紧备份恢复流程：`backup:restore` 恢复前强制执行备份包完整性校验，使用临时文件替换当前数据库，并在失败时重新初始化数据库连接，避免恢复异常后主进程持有关闭态数据库。
- 补齐前端与权限链路：备份页文案明确区分“系统备份包”与“电子档案导出”，并将“账套备份”入口调整到账套设置权限范围内，修复前后端权限语义不一致问题。
- 同步更新 `prds/prd.md`、`prds/PROJECT_SPEC.md`、`prds/合规整改计划.md`，将备份模块状态改为已支持备份包生成、manifest 校验与整库恢复。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 新增账套名称唯一性约束：主进程在 `ledger:create` 与 `ledger:update` 中统一拦截与既有账套完全同名的名称，前端新建账套弹窗也会提前提示重复名。
- 打通备份自定义路径：`backup:create` 现支持选择自定义保存目录并记忆最近目录，备份列表保留原有记录恢复能力，同时新增“从路径恢复”入口，允许直接选择任意备份包目录发起恢复。
- 打通归档自定义路径：`archive:export` 现支持选择自定义导出目录并记忆最近目录，导出记录继续落库到 `archive_exports`。
- 新增回归测试：补充 `ledgerNaming`、`pathPreference`、`backupRecovery` 用例，覆盖重名拦截、路径记忆与备份包目录解析。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 完善电子档案校验功能：`archiveExport` 新增包级校验能力，覆盖 manifest 哈希、必要导出文件存在性，以及 `original-vouchers` 原始凭证目录数量核对。
- 打通 `archive:validate` IPC、preload 类型和备份归档页前端按钮，电子档案导出记录现可进入 `validated/failed` 状态并保留校验时间。
- 为 `archive_exports` 补充 `validated_at` 兼容字段，确保旧库升级后也能记录电子档案校验时间。
- 同步更新 `prds/prd.md`、`prds/PROJECT_SPEC.md`、`prds/合规整改计划.md`，将电子档案模块状态改为“已支持导出与包级校验”。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 将备份与电子档案校验时间统一切换为本地时间写入：`backup:validate`、`archive:validate` 不再使用 SQLite UTC 时间戳，页面展示与用户本地时间一致。
- 新增旧版本清理能力：`backup:delete` 与 `archive:delete` 现允许删除存在更新版本时的旧备份/旧归档，并明确阻止删除最新版本。
- 重构备份/归档时间选择交互：备份页改为“已结账会计期间”与“可归档年度”选择器，默认分别指向最近闭账期间和最近可归档年度，不再允许手工录入。
- 为备份记录补充 `backup_period` 字段，并新增 `localTime`、`versionRetention`、`backupSelection` 回归测试，覆盖本地时间格式、版本保留规则与闭账期间/年度默认选择逻辑。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 修复 `backup:create` 插入 `backup_packages` 时的 SQL 占位符错误：在新增 `backup_period` 字段后，`VALUES` 子句少了一个 `file_size` 占位符，导致新建备份时报 “8 values for 9 columns”。
- 调整 `src/main/ipc/backup.ts` 的插入 SQL，保证 `ledger_id / backup_period / fiscal_year / backup_path / manifest_path / checksum / file_size / status / created_by` 九列与八个参数加固定状态值严格对应。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 调整“系统备份包”和“电子档案导出”列表展示：记录卡片现仅显示包件名称和创建时间戳，不再显示 `#id` 序号以及其余详细字段。
- 新增“详细信息”弹框：备份/归档卡片点击“详细信息”后，会在独立信息框中展示状态、校验时间、路径、校验值、文件大小等完整信息。
- 新增 `backupRecordDisplay` 工具与回归测试，统一备份包件名称取父目录名、归档包件名称取导出目录名，并为缺省路径提供稳定兜底名称。
- 完成验证：`npm run typecheck`、`npm test`。

2026-03-13

- 核查并修复账套备份页白屏：确认根因是 `src/renderer/src/pages/backupRecordDisplay.ts` 在 renderer 侧错误引入 `node:path`，导致 Vite 将其 externalize 到浏览器兼容层并在页面打开时触发运行期异常。
- 将包件名称提取逻辑重写为纯字符串路径解析，不再依赖任何 Node 内建模块，保持备份包取父目录名、归档包取导出目录名的展示规则不变。
- 完成验证：`npm test -- src/renderer/src/pages/backupRecordDisplay.test.ts`、`npm run build`。

2026-03-13

- 统一备份包/档案包卡片布局为三行结构：第一行居中标题仅显示会计期间年/月（归档为年度），第二行仅显示时间戳，第三行固定四个按钮槽位。
- 去除卡片上的 `#id` 序号与其他详情字段，新增“详细信息”弹框承载路径、状态、校验时间、校验值、文件大小、项目数量等完整信息。
- 新增最新版本按钮状态控制：不可删除的最新包件，其“删除旧版”按钮保持灰色禁用；档案包卡片为保持布局统一，保留禁用态“恢复”按钮占位。
- 新增 `backupCardLayout` 回归测试，覆盖标题格式化与最新版本识别逻辑。
- 完成验证：`npm run typecheck`、`npm test`。
