# AGENTS.md

全局指令：

- 每次新对话开始前，先读取 `prds/PROJECT_SPEC.md`，确保背景信息一致。
- 如发生重大变化（例如架构、数据模型、权限模型、核心流程变化），必须先更新 `prds/PROJECT_SPEC.md` 再继续开发。
- 每次完成开发任务后，必须在 `prds/开发日志.md` 追加一条中文记录。
- 记录内容包含日期（YYYY-MM-DD）与完成要点（条目形式）。
- 开发完成任务后，必须进行测试，确保功能正常。
