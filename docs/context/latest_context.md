# 最新上下文

## 当前阶段
- Execution

## 当前任务
- 已完成：修复安装升级后用户数据与设置被新版本切换到空库的问题。

## 剩余任务
- 当前切片代码修复已完成；如需用户复测，下一步可重新打包安装包。

## 关键决策
- 打包版用户数据与设置必须独立于安装目录，升级或改安装路径都不得影响已有数据。
- 兼容迁移方向定为“旧安装目录 -> userData”，且仅在目标库不存在时执行。
- 默认日志目录也应脱离安装目录，避免升级覆盖与权限耦合。

## 问题
- 原问题根因已确认并修复：打包版主数据库曾绑定安装目录 `data`，导致升级后可能读到空库。

## 下一步
- 重新打包安装包，交给用户验证旧账套、科目、设置是否保持不变。
